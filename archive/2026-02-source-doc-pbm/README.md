# February 2026 source-document/PBM architecture

This directory preserves the earlier `source_doc_pbm/` architecture notes from February 2026.

The material describes an older public-query, PBM-submission, WHC/MySQL and shard-delivery architecture. It is retained as project history and design provenance, not as the current runtime or deployment plan.

Current architecture should be read from the repository root, `/engine/`, `/kernels/`, and `/network/`.

The original files remain unchanged inside the preserved `source_doc_pbm/` subtree, and their earlier versions remain recoverable from Git history.
